import StudentAppLayout from "@/components/layouts/studentLayout";

export default function StudentLayout({ children }: { children: React.ReactNode }) {
  return <StudentAppLayout>{children}</StudentAppLayout>;
}
